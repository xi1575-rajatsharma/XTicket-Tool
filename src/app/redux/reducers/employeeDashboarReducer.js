const initalState = {};

const employeeDashboardReducer = (state = initalState, action) => {
  switch (action.type) {
    default:
      return { ...state };
  }
};

export default employeeDashboardReducer;
